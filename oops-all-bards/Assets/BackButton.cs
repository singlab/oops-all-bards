using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class BackButton : MonoBehaviour
{
    public BasePlayer p;
    public void Execute()
    {
        /*
        Debug.Log("back button is doing something");
        CombatManager.Instance.combatQueue = new CombatQueue();
        CombatManager.Instance.combatQueue.Clear();
        CombatManager.Instance.PushAndCreateCombatQueueable(new PlayerTurn(p));

        gameObject.GetComponent<BasePlayer>().OwnsTurn = true;
        */
    }
}
